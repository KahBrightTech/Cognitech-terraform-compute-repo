name: Configure-servers-int-production-shared

on:
  # Trigger mode: both
  # - manual: workflow_dispatch only
  # - automatic: workflow_run only
  # - both: both triggers enabled

  # Manual trigger
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        type: choice
        default: 'health-check'
        options:
          - health-check
          - configure
          - both

  # Automatic trigger: runs after the deploy workflow completes on main
  workflow_run:
    workflows: ["Deploy-primary-int-production-deploy-tenants-shared"]
    types:
      - completed
    branches:
      - main

env:
  IAM_ROLE: arn:aws:iam::271457809232:role/int-prod-OIDCGitHubRole-role
  REGION: us-east-1
  INSTANCE_TAG_PATTERN: INTP-SHARED-*
  SSM_DOCUMENT_WINDOWS: intp-use1-Windows-Banner-Config
  SSM_DOCUMENT_HEALTH_CHECK: AWS-RunPowerShellScript
  WAIT_TIME: 60

permissions:
  id-token: write
  contents: read

jobs:
  # Health check job - verifies instances are ready
  health-check:
    name: Health Check Instances
    runs-on: ubuntu-latest
    # Run if: manual dispatch with health-check/both, OR automatic trigger
    if: |
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.action == 'health-check' || github.event.inputs.action == 'both')) ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1.7.0
        with:
          role-to-assume: ${{ env.IAM_ROLE }}
          aws-region: ${{ env.REGION }}

      - name: Wait for instances to be ready
        run: |
          echo "Waiting ${{ env.WAIT_TIME }} seconds for instances to initialize..."
          sleep ${{ env.WAIT_TIME }}

      - name: Get instance IDs
        id: instances
        run: |
          INSTANCE_IDS=$(aws ec2 describe-instances \
            --filters "Name=tag:Name,Values=${{ env.INSTANCE_TAG_PATTERN }}" \
                      "Name=instance-state-name,Values=running" \
            --query "Reservations[*].Instances[*].InstanceId" \
            --output text)

          if [ -z "$INSTANCE_IDS" ]; then
            echo "ERROR: No running instances found matching pattern: ${{ env.INSTANCE_TAG_PATTERN }}"
            exit 1
          fi

          echo "Found instances: $INSTANCE_IDS"
          echo "instance_ids=$INSTANCE_IDS" >> $GITHUB_OUTPUT

          # Get instance details
          aws ec2 describe-instances \
            --instance-ids $INSTANCE_IDS \
            --query 'Reservations[*].Instances[*].[InstanceId,Tags[?Key==`Name`].Value|[0],State.Name,PrivateIpAddress]' \
            --output table

      - name: Check SSM connectivity
        run: |
          INSTANCE_IDS="${{ steps.instances.outputs.instance_ids }}"

          echo "Checking SSM agent connectivity..."
          for instance_id in $INSTANCE_IDS; do
            SSM_STATUS=$(aws ssm describe-instance-information \
              --filters "Key=InstanceIds,Values=$instance_id" \
              --query "InstanceInformationList[0].PingStatus" \
              --output text)

            if [ "$SSM_STATUS" != "Online" ]; then
              echo "WARNING: Instance $instance_id SSM status: $SSM_STATUS"
              echo "Ensure the instance has IAM role with AmazonSSMManagedInstanceCore policy"
            else
              echo "✓ Instance $instance_id is SSM connected"
            fi
          done

      - name: Run health check commands
        run: |
          INSTANCE_IDS="${{ steps.instances.outputs.instance_ids }}"

          echo "Running health checks on instances..."
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids $INSTANCE_IDS \
            --document-name "${{ env.SSM_DOCUMENT_HEALTH_CHECK }}" \
            --parameters 'commands=["Write-Output \"Health check complete\""]' \
            --comment "Health check from GitHub Actions" \
            --query "Command.CommandId" \
            --output text)

          echo "Command ID: $COMMAND_ID"

          # Wait for command to complete
          echo "Waiting for command to complete..."
          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id $(echo $INSTANCE_IDS | awk '{print $1}') \
            --max-attempts 30 \
            --delay 10 || true

          # Get results for each instance
          for instance_id in $INSTANCE_IDS; do
            echo ""
            echo "=== Results for $instance_id ==="
            aws ssm get-command-invocation \
              --command-id "$COMMAND_ID" \
              --instance-id "$instance_id" \
              --query '[Status,StandardOutputContent,StandardErrorContent]' \
              --output text
          done

  # Configure job - runs Windows Banner configuration
  configure:
    name: Configure Instances
    runs-on: ubuntu-latest
    needs: health-check
    # Run if: manual dispatch with configure/both
    if: |
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.action == 'configure' || github.event.inputs.action == 'both')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1.7.0
        with:
          role-to-assume: ${{ env.IAM_ROLE }}
          aws-region: ${{ env.REGION }}

      - name: Get instance IDs
        id: instances
        run: |
          INSTANCE_IDS=$(aws ec2 describe-instances \
            --filters "Name=tag:Name,Values=${{ env.INSTANCE_TAG_PATTERN }}" \
                      "Name=instance-state-name,Values=running" \
            --query "Reservations[*].Instances[*].InstanceId" \
            --output text)

          if [ -z "$INSTANCE_IDS" ]; then
            echo "ERROR: No running instances found"
            exit 1
          fi

          echo "instance_ids=$INSTANCE_IDS" >> $GITHUB_OUTPUT

      - name: Run Windows Banner configuration
        run: |
          INSTANCE_IDS="${{ steps.instances.outputs.instance_ids }}"

          echo "Running Windows Banner configuration on instances..."
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids $INSTANCE_IDS \
            --document-name "${{ env.SSM_DOCUMENT_WINDOWS }}" \
            --comment "Windows Banner configuration from GitHub Actions" \
            --query "Command.CommandId" \
            --output text)

          echo "Command ID: $COMMAND_ID"

          # Wait for command to complete
          echo "Waiting for command to complete..."
          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id $(echo $INSTANCE_IDS | awk '{print $1}') \
            --max-attempts 30 \
            --delay 10 || true

          # Get results for each instance
          for instance_id in $INSTANCE_IDS; do
            echo ""
            echo "=== Configuration results for $instance_id ==="
            OUTPUT=$(aws ssm get-command-invocation \
              --command-id "$COMMAND_ID" \
              --instance-id "$instance_id" \
              --query '[Status,StandardOutputContent,StandardErrorContent]' \
              --output text)

            echo "$OUTPUT"

            # Check if command failed
            STATUS=$(echo "$OUTPUT" | awk '{print $1}')
            if [ "$STATUS" != "Success" ]; then
              echo "ERROR: Configuration failed for $instance_id"
              exit 1
            fi
          done

          echo "✓ Windows Banner configuration completed successfully on all instances"
